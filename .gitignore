.gradle/
build/
out/
bin/

.idea/
*.iml
.vscode/
.DS_Store
node_modules/

*.log
.env
.env.*
!.env.example

# Internal audit reports — kept local, never tracked (zero AI-tooling traces in the repo)
docs/audit-*.md

# Серверные правки к compose (порты, restart-политика) — только локально
docker-compose.override.yml
